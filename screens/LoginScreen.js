import React,{useState} from 'react';
import { Text, View, FormControl, ScrollView } from 'native-base';


export const LoginScreen = props => {


    return (
        <ScrollView>
            
        </ScrollView>
    );
}

export default LoginScreen;